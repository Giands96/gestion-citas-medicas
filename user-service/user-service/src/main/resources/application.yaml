server:
  port: 8082

spring:
  application:
    name: user-service
  datasource:
    url: jdbc:postgresql://localhost:5432/clinica_db
    username: admin
    password: admin123
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    url: jdbc:postgresql://localhost:5432/clinica_db
    user: admin
    password: admin123
  jpa:
    hibernate:
        ddl-auto: update
    properties:
        hibernate:
          default_schema: user_service
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true

jwt:
  secret: ${JWT_SECRET:PrOY3ct0nlyS3cr3tK3yPrOY3ct0nlyS3cr3tK3y}
  expiration: ${JWT_EXPIRATION:86400000}